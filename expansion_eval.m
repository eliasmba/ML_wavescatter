function [fx] = expansion_eval(u, Nres, N_multi, centers, x, k)
%evaluate function in coefficeints (u)_m*l
%in spherical harmonics basis
%at point x

assert(length(u) == n*N_multi);
assert(length(centers) ==n);

end 
end